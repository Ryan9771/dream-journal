rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // The browser never reads journals directly. All access goes through the
    // authenticated API, whose Admin SDK is governed by IAM.
    match /users/{userId}/{document=**} {
      allow read, write: if false;
    }

    // Server-managed access tiers are never readable or writable by clients.
    match /insightAccess/{document=**} {
      allow read, write: if false;
    }
  }
}
